//In that file, define a function named math that takes three arguments.  
 // It's important for you to understand that arguments names are only used  
  //to reference them.  

  function math(x, y, z) {
    return (y*z) + x
  }
  //Name each argument as you like.  
   
  console.log(math(53,61,67))
  //Within the math function, return the value obtained from multiplying  
  //the second and third arguments and adding that result to the first  
  //argument.  
   
 
